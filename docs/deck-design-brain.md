# Deck design brain

How to decide what a slide should look like and how to build it. This is the *reasoning* layer for presentation decks; the hard layout rules (fixed 1280×720 canvas, 48px padding, grid, title scale) live in `CLAUDE.md` → "Presentation decks", and the mechanics of `.deck-slide` live in `base/deck-layout.css`. Read those first; this doc tells you how to *think*.

The core principle (from CLAUDE.md): **compose every slide from design tokens and atomic components upward, and be creative with anything larger.** There are no deck-specific components. Every recipe below is a *composition* of existing atomic/page-level pieces and raw tokens — reshape them freely (horizontal cards, mixed sub-components, side-by-side or stacked), never treat a web component as a fixed unit.

**When a reshape recurs**, promote it to a documented component variant (in the component's own CSS) rather than re-declaring the same override in slide after slide — e.g. a horizontal card layout, or a brand-coloured stat figure. Slide-local CSS is fine for a genuine one-off; it is a smell once you've written it twice.

This is a living document. New reference slides refine it — see "E. Extending the brain" at the end.

---

## A. Design DNA — the invariants

These are what make a slide read as Smartcat. Hold them constant no matter what you're building. Values are the source-of-truth tokens; the hex shown is for recognition only.

- **Type.** Headings in Plus Jakarta Sans Bold (`--family-heading`), body in Inter Regular (`--family-body`). Headlines are large and tight, and frequently **end with a period** as a stylistic device — "Deck heading.", "Thank you.". Match that voice. Those two families are the whole set — never a third, and no italic serif, monospace chip or other guest face dropped into a headline. Tracking is never hand-set — every size ships with its own `--letter-spacing-*` token, and there is no per-theme weight adjustment on a dark slide either. **Body copy is set at 1.15 leading and never at "single"**: paragraphs, card copy, captions, bullets and table cells all take it. A slide is read at a distance, and single-spaced 18px body sets far too tight for that; headings and display figures keep their own tight default.
- **Five levels of type, each with one job.** A slide uses at most these, top to bottom: **Display** (64px) for a statement that *is* the slide — a divider, a manifesto, a chapter numeral; **H1** (48px) for the slide title; the **lead** (H3, 24px, heading face, primary) for the thesis sentence that opens a body of copy; **body** (18px Inter, secondary) for the copy itself; **caption** (14px, tertiary/secondary) for sources, metadata and feature labels. Most slides use three of the five; a text-led slide uses four (title → lead → body → caption). The lead level is what the external reference set does on nearly every text slide and what a generated deck lacked: a paragraph that opens at body size has no thesis, so the eye has nowhere to start. It replaces nothing — it sits between the title and the body, and it is set in the heading face because the system has no regular-weight large text (the tokens run H1–H3 bold, H4–H5 regular at body sizes).
- **Body copy takes a reading measure, never the slide width.** A paragraph runs 45–65 characters a line — roughly half the content width at 18px. Two narrow columns of body copy under a statement (the manifesto shape) or beside a lead are the reference way to carry 100–200 words; a full-width paragraph is never one of the options. Columns separated by a hairline (`--border-divider-default`) rather than a wider gap are the editorial form; the hairline is the one line element the system draws besides connectors and table rules.
- **A ruled list is bracketed, roomy, and led by an icon.** Whenever hairlines separate the lines of a list — a divider's chapter contents, a contents page, any run of one-line points — the rules go **above the first row and under every row including the last**, so the list reads as one object with a top and a bottom edge. Rules only *between* the lines, at a tight line pitch, read as underlined sentences instead (user correction 2026-09-22). Give each row real height — roughly 56px for a single line, three times the type size — and lead it with a design-system icon at 20px in the **same muted tone as the text**, not brand purple: these lists sit on dividers and contents pages where the title is already the slide's one accent.
- **Micro type has one job and one place.** Sources, footnotes and disclaimers are caption scale in `--content-static-tertiary`, parked in the same position on every slide that carries them (normally bottom-left, under the content). Its smallness is the point — never promote it to body size to fill space, and never shrink it below the caption token to fit.
- **Title placement.** The **cover** anchors its headline left-aligned in the **lower-middle band** of the canvas — clearly below center, but with visible room left below it for a subtitle or closing metadata, never flush at the very bottom edge (see the Cover recipe in section C for why the exact position varies). **Every other slide** anchors its title **top-left**, flush at the 48px padding origin.
- **Whitespace is deliberate.** Most content slides fill only ~60–70% of the canvas. The title sits alone at the top with a large gap (the fixed 80px heading→content gap) before content, and content rarely runs to the bottom edge. Do not fill empty space just because it's there — restraint is the look.
- **A slide needs enough content to earn the canvas.** This is different from "whitespace is deliberate" above, which is about restraint in an otherwise-complete slide. This is about a slide that never had enough to begin with: a bare title + one short sentence, or any single skinny element with the bottom half of the slide empty, isn't a slide — it's a fragment. Before treating something as its own slide, check whether it actually fills a meaningful share of the canvas at a normal information density. If it doesn't, fold it into a neighboring slide under one shared idea instead — a one-line claim becomes the subtitle under a heading+paragraph, or a lone stat becomes the third item in a numbers row.
- **Silhouette variety is a rule with numbers, not a mood.** A slide's *silhouette* is the shape a viewer sees at thumbnail scale before reading a word. The deck engine names them — `statement`, `prose`, `row` (one row of cards or stats), `grid` (two or more rows), `rows` (numbered/icon rows), `split`, `stack`, `banner`, `flow`, `table`, `quote` — and `check_layout_variety` enforces three rules: **no two adjacent slides share a silhouette; no silhouette carries more than a third of the content slides (6+); a deck of 8+ content slides uses at least four.** Two earlier passes at this (see the changelog) said "vary the layout" in words and generated decks still came out as a title over one strip of equal boxes, slide after slide — because every builder produced that silhouette and nothing asked. The failure was never that cards or a paragraph are wrong shapes; it was reaching for the *same* one by reflex. So the shape is now chosen **per slide, from the content's parts** (section D, step 3), and the whole list is checked before anything is built. Variety for its own sake is still not the goal: a slide whose content is one paragraph stays a paragraph; the rule only stops the sixth paragraph in a row.
- **A content block earns the canvas or changes shape.** "Size a card to its content" was added to stop cards stretching to the slide floor. Without a floor it produced the opposite defect: a 120px strip of squat, caption-size cards floating in 500px of empty slide. The reference content slides fill roughly **50–70%** of the area under the title. So: a block sizes to its content **but never below half of that area** (the engine's `MIN_CONTENT_FILL`; `check_canvas_fill` flags anything under 40%). When the floor is doing most of the work — cards puffed up around two lines of copy — the content is too thin for that shape: go to a 2×2 grid with body-size copy, a split with the copy beside it, a stack with the detail underneath, or merge with a neighbour. Deliberately sparse shapes (statement, stand-alone quote, prose) are exempt; they are meant to breathe.
- **Color.**
  - Neutrals: near-black `#13101C` text / off-white + gray surfaces in light mode; white text / near-black surfaces in dark mode. Always via `--content-*` and `--background-static-gray-layer-*` tokens.
  - **Brand gradient** (the signature accent): white → pink `--color-non-semantic-pink-70` (`#c326ed`) → purple `#731EF2` → indigo, left-to-right, always as a **flat linear fill with a sharp edge**. Used for hero moments — section-divider backgrounds, the testimonial/comparison "hero" panel, chart fills. Not for ordinary content, and **never a blurred radial blob, orb, or glow** — that generic "AI-marketing" mesh-gradient look is explicitly banned (see CLAUDE.md → "Section backgrounds beyond the gray layers"). A decorative accent, if one is wanted, is a flat brand-purple shape with a hard edge — not a soft gradient glow. (The hex here was corrected 2026-09-18 — `#DA00FE` doesn't exist in `tokens/globals.css`'s pink ramp; pink-70 is the actual token this was describing, confirmed by pixel-sampling the cover gradient below, which uses the same pink family.)
  - **Cover gradient** (cover-only, distinct from the brand gradient above): a **vertical** ramp — flat dark (`--background-static-gray-layer-0`, dark mode, ≈`#1a1824`) for the top ~45–50% of the canvas, then a smooth transition down through brand purple (`#731ef2`, ~75–85% down) into pink (`--color-non-semantic-pink-70`/`60`, `#c326ed`–`#cf48f4`) at the very bottom edge. Confirmed by pixel-sampling three real cover slides (`references/decks/cover/`) — all three use the identical ramp. This is the **only** place this specific vertical dark→purple→pink treatment appears; do not reuse it as a general "dark gradient" for ordinary content slides (those use the horizontal brand gradient above, or a solid fill), and never substitute a soft radial blob for it.
  - **Brand purple** `#731EF2` for single-color brand emphasis — stat figures, chart primaries, active/"our" elements, number badges. For text and figures the token is **`--content-static-brand`** (not a background token) — and it is **theme-aware**: purple-70 in light mode, **purple-50 in dark**. Purple-70 on a near-black slide is too dark to read, which is exactly why the token carries a dark-mode value; a brand-coloured *fill* (a numbered badge, a pill) keeps purple-70 in both themes, since it carries white on top.
  - **Semantic green/red** (`--...-positive` / `--...-negative`) **only for genuinely positive/negative meaning** — highlights vs lowlights, Gantt done vs pending, thumbs up/down. Never decoratively.
  - **One accent job per slide.** The brand accent does exactly *one* of these at a time: the key figure *or* one accent word in the headline — never both. Never a label above the title: a line set above a heading is an eyebrow whatever colour or casing it carries, and eyebrows are banned system-wide (CLAUDE.md → "Text casing"). Some reference slides (`references/decks/dark/exec-summary-3col.jpg`, `workflow-steps.jpg`) do carry one — they predate the ban; do not copy it. Same logic one level up: **one chromatic idea per slide**. If the layout is busy the type stays plain; if the type is the artwork the ground stays flat.
  - **Accent word in a headline.** Setting one word or short phrase of a headline in `--content-static-brand` is sanctioned — it is the deck's instance of the same device as a document cover's `.doc-hero__title-accent` and a social canvas's `.social-canvas__accent`. It is a *title* treatment, not prose emphasis, so the no-inline-bold rule doesn't reach it. One per slide, headline only, never inside body copy.
  - **Tonal ramp.** A run of surfaces stepping through `--background-static-brand-layer-1` → `-2` → `-3` → `-inverted` may encode **real rank or escalation** across a card set or stat ladder (low→high, stage 1→4). Only when the ordering is genuine — used decoratively it is just four different purples. The ramp inverts in dark mode (the layer tokens flip), which is correct: it stays monotone either way.
- **Text never leaves its box.** A paragraph that runs past the bottom of its card, a caption that wraps out of a stat card, a label whose period drops to its own line — each is a hard defect, not a polish item (user correction 2026-09-21). The fix is never to shrink the type below its token: shorten the copy, switch the card to horizontal, use fewer items, or split the slide. `check_text_fit` estimates every text box against the room it was given and flags the overrun; the builders reshape a card set that would not fit before they ever squeeze it.
- **Tone on dark slides.** Headings and the captions that matter (a stat label, an attribution name) are **full white** (`--content-static-primary`); every run of body copy — paragraphs, card copy, details, and **bullets that continue a paragraph** — is the secondary shade (`--content-static-secondary`). A bullet list under a paragraph is part of that paragraph and takes its colour; setting it white makes it read as a second heading (user correction 2026-09-21). The same split holds in light mode with near-black and gray.
- **Emphasis ladder — in this order.** Size, then weight, then tone (drop the supporting half to `--content-static-secondary`), then the one accent word or figure. That is the whole vocabulary. Two token-only devices are worth naming because they buy hierarchy with no size change: the **two-tone title** (line 1 in `--content-static-primary`, line 2 in `--content-static-secondary`, same size and weight) and the **two-tone paragraph** (lead sentence primary, the rest secondary, one size). Both split at a whole-line or whole-sentence boundary — never mid-sentence, which would just be inline emphasis. Not in the vocabulary: underline (it means "link" in this system), outline or ghost type, marker-pen highlights, and inline bold inside a sentence (CLAUDE.md → "Text casing").
- **Surfaces (panels/cards).** Rounded corners, generous internal padding. On **light** backgrounds → solid gray layer-1 panels. On **dark** backgrounds → the frosted alpha panel in CSS; in pptx, which has no backdrop blur, its flat stand-in is **gray-80 (`#1F1D2A`)**, one step off the slide's own gray-90 ground. Any lighter and the panel reads as a grey box floating on the slide instead of a surface sunk into it. One featured panel per comparison may use the brand gradient fill. **Every content card carries a visual anchor** — a design-system icon or a numbered circle badge — never heading+paragraph text alone. The one exception is a stat card (Numbers/stats recipe): the large figure itself is the anchor, so no icon or number is needed there. **A narrow card takes a smaller heading.** Below roughly 260px — a card panel inside a split, a 4-up row — the H3 heading wraps a three-word phrase onto three lines and swamps the copy beneath it; step it down to H4. The same applies to a **stat figure on a narrow card**: a Display figure needs about 170px for something like "400%", so below that width it steps down to H1 rather than wrapping its last glyph out of the card. It is still the largest thing on the card, so it still reads as the anchor. **Size a card to its own content, within the canvas-fill floor** (Design DNA, "A content block earns the canvas or changes shape") — a card with one short sentence is shorter than one with two, the row or grid is centred in the space under the title, and it never shrinks to a strip: at least half that space, or the shape changes. **Card copy sets at body size (18px) on any card 360px or wider**; the reference cards all carry body-size copy, and a 3-up or 2×2 grid at caption size reads as a row of footnotes — the single biggest reason a generated card row looked thin next to the references. Only a 4-up row or a split's narrow card panel drops to caption. **Four cards with real paragraphs are a 2×2 grid, not a 4-up row**; five or six are two rows of three (benefit-cards.jpg, compliance-risk-grid.jpg). A card's anchor may also be a **check badge** (a filled circle holding the design system's check icon) when the row is a confirmation list — same slot as an icon or number badge. **The two anchor kinds are not the same size or colour.** A numbered badge is the smaller disc, solid brand purple with a white numeral, in both themes. An **icon** anchor is a visibly larger disc, and it reads against the *surface* it sits on rather than the theme: on any card whose fill is not pure white — which is every card in both themes — the disc is **white** with a brand-purple glyph, falling back to the brand tint only on a genuinely white surface, where a white disc would vanish. Both kinds take the same generous gap down to the heading; a badge crowding its own text is the single most common way a card row looks unconsidered. When a card set is deliberately uneven — a bento of several small cards and one hero card anchoring the grid — **vary the card size, never the radius or the gap**: one radius token and one gutter value across the whole grid is what keeps it reading as a single object. A row of images obeys the same law: **same aspect ratio, same radius, same treatment on every crop** — equal crops are what make twelve mismatched headshots or logos read as one set.
- **A missing image or logo asset gets a placeholder, not a gap.** Where a real asset belongs — a customer logo, a partner/tool icon tile, a product screenshot — and isn't available at build time, render a visible placeholder instead of omitting the visual or leaving bare text: a dashed-border panel (gray layer-1 fill, no image) holding the design system's `placeholder` icon centered, with a small caption naming what belongs there (e.g. "Logo: Adobe Experience Manager", "Screenshot: workspace setup"). This keeps the slide's intended composition legible and gives whoever finishes the deck an exact, labeled slot to drop the real asset into — never fewer elements than the layout calls for.
- **Imagery.** Slides draw on the same sanctioned visual types as the rest of the system (CLAUDE.md → "Visual type per media slot") — product-UI screenshot, conceptual illustration, icon tile, design-system vector icon, monochrome customer logo, circle-crop portrait. **Never generic stock photography**, and never a treatment the system doesn't have (duotone, halftone, macro texture, abstract 3D); pick one treatment and hold it across the whole deck. **Screenshots**: rounded corners, no border and no shadow, cropped to the part that proves the point — the hairline-bordered, soft-shadowed frame is a *document*-tier rule, not a deck one (see "No drop shadows" above). **Type over an image**, in escalating order: pick or crop to an image with a quiet zone → set the type on its own surface over the image (the frosted alpha panel from Surfaces above) → split the canvas and stop fighting it. **Never fix legibility on the type** — no drop shadows, outlines or strokes; fix the ground. One cell of an otherwise-regular grid may be an image instead of a card — that is the "hierarchy within a repeated component" device below, used deliberately and once.
- **A slide about a product surface reserves room for it.** When a slide's claim is about something the product actually *looks* like — the conversation you drop a file into, the side-by-side review view, a dashboard — the layout holds a **reserved image slot** for a screenshot inserted by hand later (`draw_image_slot`). Three things follow from that:
  - **This is not the missing-asset placeholder.** That one (`add_placeholder`, above) stands in for an asset we meant to have and didn't. A reserved slot is a composition decision made before anyone looks for an image — **not having a real screenshot at build time is the normal case for this mechanism, never a reason to withhold it.** The deck's job is to reserve the space and describe the shot; producing the actual image is a separate, later step for someone else. They are drawn differently on purpose — the reserved slot is brand-purple and states its exact pixel size and aspect ratio, because someone has to go and produce an image of precisely that shape. **Never label a slot with a ratio it isn't**: a banner marked "16:9" that is really 3.95:1 sends the exporter after the wrong crop, which is the one failure the label exists to prevent.
  - **Only where a specific feature is named.** "See before and after, side by side" earns a slot. "SOC 2 compliant" does not, and neither does a benefits row or a list of file formats. A screenshot on every slide is the generic SaaS look, and it is worse than none.
  - **Screenshots are inserted, never drawn.** Decks embed the real image, the way documents do — do **not** reconstruct product UI from atomics. That is the *social* tier's rule, for a thumbnail-sized illustration of the product; a deck shows the product.
- **Four slot shapes, all taken from the reference decks** (`Deck refs/Image placement`), and the slot is sized to the image, never the other way round:
  - **Image-led split — the default** (`add_image_split`). The slot fills the **full height at the image's own aspect ratio**, and the copy (a paragraph, a column of cards, stats) takes the width that is left. **When the whole slide is a title, a paragraph and a screenshot, the title goes in the left column with the copy** and the shot runs the full slide height beside it, from the top padding down (`title_column`, on by default for a bare text panel — user correction 2026-09-22). A full-width title there costs the image a third of its height, so the slot flattens into a wide box the picture cannot fill, and a band of dead space opens under the headline. The title is still top-left at the padding origin; it is measured to the column rather than the slide, so it wraps to two or three lines — that is the intended look, not a defect. Keep the full-width title when the copy is a card or stat column, where it reads correctly over the panel. **Every finished shot in `images/promo-ui-mockups/` is square (1080×1080)**, so for those the slot is a full-height square — roughly 45% of the content width — with the copy vertically centred on it. A square dropped into a 3:2 box or a banner covers barely half the slot and leaves dead flanks; the engine reports that as a poor fit.
  - **Side panel** — an ordinary `add_split` with an `image` panel: the slot holds 3:2 (or an explicit `aspect`) and grows to match a taller neighbour, so the two sides share both edges. For a landscape screenshot beside a tall text panel.
  - **Banner** — the slot runs the full content width under the title (`add_image_banner`), and it is only ever right for a **genuinely wide** screenshot (2:1 or wider) whose shape actually fills that box. Not for a square or 4:3 shot, and not merely because a slide has one supporting line: `add_image_banner` now delegates to the image-led split above unless it is given a wide `aspect`, since the flat-banner-with-a-paragraph slide was the shape being produced by mistake. Declare the `aspect` so the slot is labelled with the crop someone has to export.
  - **Grid cell** — one or more cells of a card row given over to imagery, at exactly the card's own size, radius and gutter, so the row still reads as one object. Pass `image` instead of `heading` on that card.
- **A deck about a covered product plans its slides around the shots that exist.** Before writing the slide list, print the catalog (`list_promo_images(root, product)`) and read the descriptions: each finished shot is a candidate slide — the surface it shows is a claim the deck can make with the product visible. A product deck that leaves most of its shots on the shelf is under-showing the product; `run_all_checks` lists the unused ones on every build so that is a decision, not an oversight. Reservation still comes first and matching second (CLAUDE.md "Promo UI mockups"): write each slot's caption in the folder's own vocabulary — the product name plus the surface, in the words the filenames use ("Content Translator Coworker editor", "configuring your chief of staff through chat") — and let `fill_matched_slots` do the lookup in one pass.
- **Add a slot freely, whether or not a screenshot exists yet — the obligation is to report it, not to avoid it.** Lacking the real asset at build time is the ordinary case for a reserved slot, not an exception to the rule; skipping the slot because the screenshot isn't ready yet defeats the entire mechanism. What must never happen is *silent* delivery: `image_slots(prs)` returns every reserved slot with its slide number and caption — hand that over as a shot list on every handoff, and say plainly the deck isn't finished until they're filled. A reported slot is normal; an unreported one is the actual failure.
- **Gutter vs. slide padding.** The 8px gutter (`--grid-gutter`) is for the gap between two adjacent elements that **each carry their own visible surface** — stacked cards, side-by-side panels — where the tight gap reads as edges within one grid. It assumes a surface on both sides of the gap. When one side of a gap is **bare** (no background of its own — a hero stat, a bare heading, plain text) next to a paneled neighbour, there is no second edge for the gutter to relate to; use the slide's own padding value instead (**48px**, `--spacing-9`), so the bare content reads as sitting in the slide's whitespace rather than glued to the panel. Apply this per adjacency, not globally — override that one row's gap locally; do not redefine `--grid-gutter` itself, since other rows/stacks in the same slide may still be panel-to-panel and need the true 8px gutter.
- **No drop shadows — ever.** Not on cards, panels, icon tiles, arrows, tables or images. A surface is separated from its ground by fill and radius alone, which is how every reference slide does it. This needs saying because PowerPoint applies its theme's default shape effects to anything that does not explicitly opt out — the flow-chain arrows shipped with a shadow nobody authored. `deck_pptx.py` now clears effects on every shape it creates and exposes `strip_shadows(prs)` as a final sweep. (Scope: decks. The document tier's `.doc-screenshot` frame and the onepager tier's card shadows are unchanged — they are printed formats with their own rules.)
- **No chrome — ever.** Smartcat slides carry no running head, no page number, no per-slide logo, no section rail, tab strip or progress indicator. Confirmed across every reference slide in `references/decks/`: the wordmark appears on the cover, the monogram on a comparison's "ours" panel, and nowhere else. Navigation is carried entirely by the section dividers and the light/dark banding (section B) — do not invent a corner kit, a footer rule or a "you are here" marker. Any interactive version of one (jump-to pills, a "back to contents" link) is doubly out, per the next bullet.
- **No interactive UI — ever.** A deck is presented and viewed, not operated. The button paradigm (`.btn`, hover/active states, a click affordance) belongs to web UI and has no place on a slide. For a call to action, next step, or contact info, set it as **plain or link-styled text** (`--content-link-default` for a hyperlink-like emphasis, or just regular text) — never a pill, fill, border, or anything shaped like a clickable button. This applies everywhere a web instinct might reach for `.btn`, most commonly the closing slide.

---

## B. Choose the background and theme

Theme and background are **two independent, composable axes** (set on `.deck-slide`: `data-theme` × `data-layer`). Decide both per slide, then keep them steady across a run of related slides.

**Theme** — decide it per *band* (a run of 2–4 related slides), not per slide, so the deck scans as chapters; section dividers are the natural place to flip. **Content slides are not "light by default."** The reference set is majority dark on content slides — `references/decks/dark/` holds more content slides than `light/` — and a deck that keeps every content slide light with only the cover and dividers dark reads flat, which is exactly what an earlier "light sandwich" rule in the skill produced. Aim for roughly even, alternating in bands: hook and proof bands lean dark, tables and trust/compliance bands lean light. `check_theme_banding` flags a deck where one theme carries more than 80% of the content slides.

**Background** — pick the least intense one that does the job:

| Background | `.deck-slide` | Use for |
|---|---|---|
| Solid white | light, layer-0 | The majority of regular light slides. |
| Solid gray | light, layer-1 | Light slides that need to differ from their neighbours; light-mode section titles. |
| Dark gradient (flat brand-gradient wash, sharp-edged) | dark | Regular dark slides; can also carry a light-mode section title. |
| Solid black | dark, layer-0 | Regular dark slides; strong section titles. |
| **Brand / inverted background** | `data-layer="brand"` (no theme flip — see note) | Hero moments only: section dividers, the closing "thank you", at most 1–2 per deck. |

Rule of thumb: **content slides** lean solid (white / gray / black); **gradients are reserved for dividers and the open/close** — and even there, a gradient is always a flat wash, never a soft blob/orb/glow (see Design DNA → Color). There is no "mesh gradient" asset in this system — do not improvise one with a CSS radial gradient or a blurred shape; that is exactly the banned pattern.

**Note on brand/inverted backgrounds.** `background-static-brand-inverted` (and every other `*-inverted` background token) is a self-contained pair with `--content-static-inverted` — it already contrasts correctly in whichever theme is ambient (dark purple + white text in light mode; pale purple + near-black text in dark mode). Do **not** add `data-theme="dark"` to "get" white text — that flips the background to the pale dark-mode value while independently flipping content to white, which is illegible. `base/deck-layout.css` handles this correctly by repointing `--content-static-primary`/`secondary` to `--content-static-inverted` on `data-layer="brand"`; when hand-building a slide outside that mechanism (e.g. authoring raw shapes), use the inverted background with the inverted content color directly, in one theme, with no flip.

---

## C. Slide-role catalog — recipes

Each role: *when to reach for it*, and *how to compose it* from tokens + atomic/existing pieces. These are starting points — combine and reshape as the content demands.

### Openers & dividers
- **Cover.** Grounded in three real reference covers (`references/decks/cover/`) — consistent across all three:
  - **Background: always the cover gradient** (Design DNA → Color), never solid or the horizontal brand gradient. Dark theme throughout, white text.
  - **Logo lockup, top-left, always present.** The Smartcat wordmark (white) alone for a generic, non-client asset (an ungated content piece, a template). When the deck is for a specific named account, pair it with that account's own logo beside it, small gap, same baseline — never Smartcat alone once a client is named, and never a decorative element added to either mark (see CLAUDE.md → "Logo").
  - **Title: large, bold, white, left-aligned, 2–3 lines with deliberately chosen line breaks** (not left to auto-wrap) — sits in the lower-middle band per Design DNA "Title placement," roughly where the gradient is transitioning from dark to purple. Its exact vertical position isn't fixed — it sits higher when a metadata block below needs room (see next), lower when the cover is otherwise bare.
  - **Optional subtitle directly under the title** — one short sentence framing the deck's purpose ("A guide for enterprise teams turning…", "A business case for governed localization…"), lighter weight than the title, in **purple-40 (`#C5B3FC`)**. Not white: the cover's two small lines are tinted, not neutral.
  - **Optional metadata block — pick at most one, or omit it entirely; never combine two:**
    - *Top-right proposal info* (formal external proposal): a thin horizontal divider rule, a short framing line ("A strategic proposal for [Client]"), another divider, then "Prepared for: [name, title]" — small, muted, right-aligned.
    - *Bottom two-column prepared-by/for* (account-team business case): near the bottom edge, two columns, each a thin divider rule over a small label ("Prepared by" / "Prepared for") with names below (a bold team/lead line, then individual names).
    - *Bottom footer line* (lightweight): copyright + confidentiality + doc type + date on one small line, separated by middle-dots — e.g. "© 2026 Smartcat, Inc. — Confidential · Strategic partnership review · June 2026." Sentence case throughout: a reference sample had its doc-type label in all-caps, which this system's text-casing rule overrides — never copy that verbatim.
    - *None* — the simplest generic cover is just logo + title + optional subtitle, nothing else.
    Whichever metadata block is used, its small type is **pink-40 (`#EDB7FB`)** — a step warmer than the purple-40 subtitle, so the cover's two quiet lines stay distinguishable from each other against the gradient.
  - No eyebrow label above the title (confirmed absent in all three references, consistent with the system-wide ban); no interactive UI.
  - Build from `heading` (title + optional subtitle) plus the logo and whichever metadata block fits; no other decorative object — restraint is still the look here, the gradient and logo lockup carry the visual interest.
- **Section divider.** One big title **top-left** (Display scale), full-bleed background, no body. This is where you flip theme/background for chapter rhythm — solid gray, dark, or brand gradient. Build from `heading` alone. Three optional devices from the reference set (`add_section_divider`), combinable, for a deck whose chapters deserve more than a bare title: a **chapter numeral** ("01", "2.0") at Display scale in brand purple at the top-left origin, with the title directly under it — never dropped to the lower band: only the cover and back cover sit low, every other slide reads from the top-left (user correction 2026-09-21, after a divider shipped with its title and list at the floor); a **ruled list** of what the chapter covers under the title — body size, secondary, one roomy row per line, each led by a muted icon, the whole list bracketed top and bottom (Design DNA, "A ruled list is bracketed"); and a **full-height image slot** filling the right ~45%, square and inside the padding — a chapter opener that shows the product surface the chapter is about, and another host for a promo shot. Nothing bleeds off the edge: the reference half-bleed photo dividers were adopted as a full-height panel inside the 48px frame, not as a bleed.
- **Manifesto** (`add_manifesto`, from the reference "headline as content" slides). No separate title: one Display-scale statement across the top ~85% of the width *is* the slide, with one or two narrow body columns under it (the detail behind the claim) and an optional caption-scale footnote bottom-left. The opening thesis of a read deck, or a chapter's one-paragraph argument. Silhouette `statement`.
- **One big statement.** A single sentence at large scale, vertically centered or top-left, nothing else. Build from `heading` (title-only) or `text-block`.
- **Opening hook — the slide after the cover.** Open on a reason to care, not on the agenda: a change in the world, a shared frustration, an unexpected number, or a question. Any role below can carry it — most often one big statement, a heading + paragraph, or a single stat. When a deck genuinely needs an agenda slide, it follows the hook rather than replacing it.
- **At-a-glance summary.** Everything the deck concludes, on one slide, before the chapters start — one numbered card per finding (the numbered-badge anchor from Design DNA), a short claim each. Use it on any deck long enough to have chapters; in a read deck (see section D) treat it as the slide most people will actually read. Grounded in `references/decks/dark/exec-summary-3col.jpg`, which pairs it with a summary paragraph panel in an asymmetric split.

### Text-forward
- **Statement + aside** (`add_statement`, from `statement-body.jpg` and the reference two-column editorial slides). Title top-left, then the argument itself set **large** — the lead paragraph in bold H2 heading type, primary colour, on the left — with the supporting copy on the right: one paragraph or several, body type, secondary, starting on the same top line. `ratio` sets the columns (6:6; 5:7 when the aside carries the detail, 7:5 when the lead is the point) and `divider=True` draws the hairline between them that the references use instead of a wider gap. This is how a prose slide lands one idea without a diagram, and the natural shape for the opening hook. Dark by default.
- **Panel headings, lead lines and feature grids.** Any split or stack panel may open with its own **H3 heading** (the reference split whose right half says "Multimodal AI is on a rapid growth trajectory" over two stats); a text panel may open with a **lead** line (H3, heading face, primary — the thesis before the body), take a **list of paragraphs**, and close with a **feature mini-grid**: up to six icon + caption-label pairs in two columns, the compact "what it does" row under a product paragraph. Together these give a single text panel four levels of type without a second component.
- **Heading + paragraph.** The statement's smaller, quieter sibling: title top-left, one paragraph in body type below after the 80px gap, occupying the left ~half. Optional subtitle line under the title (muted, `--content-static-secondary`). Build from `heading` (`data-paragraph`). For a paragraph that merely introduces what follows; if the paragraph *is* the point, it is a statement. The paragraph needs to be a real paragraph, not a single clause padding out an otherwise-empty slide — see Design DNA, "A slide needs enough content to earn the canvas." A short supporting bullet list may sit under or beside it (see next).
- **Bulleted list — a supporting element only, never the whole slide.** A column of bullets is a fine way to add a short supporting list next to a paragraph, inside a split panel, or under a heading. But a genuinely enumerable, parallel set of items (3 or more real peers) is a cards job, not a list job, even when each item is short — see "Enumerated content" below and decision-procedure step 3.
- **N columns of text.** Title + 2–3 equal text columns (each an optional sub-heading + paragraph) across a `.grid` row. Use for parallel explanations with no visual per item.

### Asymmetric & stacked layouts
These aren't a separate "purpose" the way the sections around them are — they're a reshape that applies across several purposes above and below. Reach for them when the content itself is uneven or layered; don't apply them for variety's sake to content that's genuinely parallel (that's still N cards).
- **Asymmetric split.** Two panels at an uneven width ratio (e.g. 5:7 or 4:8), not an even column division — a short narrative beside a wider card grid, or one highlighted stat beside a comparison table. Use when one side is inherently smaller (a framing sentence, a single headline figure) and the other carries the supporting detail; forcing both to the same width under-serves whichever side actually has less to say. Panel gap follows the gutter-vs-padding rule above: 8px when both sides carry their own surface, **64px** (`--spacing-11`) when one side is bare text or a bare heading — a bare column needs more air against a paneled neighbour than two panels need against each other. **Both panels share one top edge, and the pair is centered as a single block** in the space below the title. Letting each side decide its own vertical position is how a split ends up with its text pinned under the title and its cards floating in the middle.
- **Stacked composition** (`add_stack`). Two different components stacked vertically under one title, both full width — the pairings seen in the references: a **stats row over a quote bar** (`stat-cards-quote.jpg` — the quantitative and qualitative halves of one claim), a **flow chain over the screenshot** of the one step that matters, **numbered rows over a stat row**, a **card row over an image**. Use it when a slide needs both a headline element and the detail behind it, and side by side would starve one of them of width. The block sizes to content within the fill floor, and the top and bottom share the extra room in proportion. **A stack is still one idea.** A process chain and a set of feature cards are two ideas and therefore two slides — never merge them (user correction 2026-09-21; a generated deck did exactly that). The only cards that belong under a chain are ones that *quantify those same steps* — `workflow-steps.jpg`'s "where the time goes" row under the six-step chain — not the deck's feature list.
- **Image-led split** (`add_image_split`). A product-UI slot sized to the image's own aspect filling the full height under the title, with the copy beside it — a paragraph with bullets, a **vertical stack of 2–3 full-width cards** keyed to what the shot shows (never side by side — see "Surfaces"), or a stat. The default shape for any slide about a product surface, and the only correct host for the square promo shots (Design DNA, "Four slot shapes"). The image sits on the side away from the reading start unless the copy is longer than the picture is tall.
- **Hierarchy within a repeated component.** A row of otherwise-identical cards need not stay uniform — one card can be visually promoted (a brand-purple fill or border, a small "recommended"/featured marker) to read as the featured option among peers, without breaking out of the N-cards shape entirely.

### Enumerated content
- **N cards — a row or a grid (2–6).** Title + panels, each = a visual anchor (icon or numbered circle badge — see Design DNA) + sub-heading + paragraph in body type. Light → gray panels; dark → frosted alpha panels. **This is also the default recipe for any genuinely enumerable, parallel list** — a plain bullet list of several peer items reads flat; give each item a card instead. **Pick the grid by the copy, not the count**: 2–3 items → one row; 4 items with real paragraphs → a **2×2 grid** (`columns=2`, exec-summary-3col.jpg's right panel), 4 items with a line each → a 4-up row; 5–6 → **two rows of three** (benefit-cards.jpg, with a 3+2 last row kept at card width). Reshape to horizontal (media left, text right) when useful. One card may be promoted over its neighbours (`featured: True` — solid brand fill, white text, inverted badge) — see "Hierarchy within a repeated component" above — when the content has a genuine featured/recommended option; don't promote a card just to break up the row. Card height follows its own content within the fill floor — see Design DNA, "Surfaces." **Cards beside a tall neighbour — stack them, don't line them up.** Two or three cards sharing a split with a full-height screenshot must not sit side by side: each becomes a tall, narrow column with two lines of copy at the top and a floor of empty gray (user correction 2026-09-22, from a rendered slide). When the block is much taller than a side-by-side row needs, the cards go **one above the other as full-width cards** — each wide and short, the anchor over the text as usual — so the column reads as a list of points next to the picture. The engine does this automatically for 2–3 cards whenever the block is more than 1.5× the row's natural height. **Two card layouts:** *stacked* (anchor over text, the reference card) is the default everywhere; *horizontal* (anchor left, text right, both top-aligned — about half the height) is the fallback when a stacked column would not fit its room — three or more cards with real paragraphs beside an image (user correction 2026-09-21, where the text spilled out of the squeezed cards). So: spare room → vertical stack of full-width cards; overflow → horizontal cards; never a squeezed card and never a tall empty one. Pass `layout` to force either.
- **Icon cards.** The same recipe, called out separately only because the icon is the point — a distinct, meaningful icon per item rather than a generic anchor. Leads with a circular icon tile (design-system icon, brand-purple) above a caption + paragraph. Built from `cards` (`data-media="icon"`).
- **Grouped tiles.** A categorized enumeration — several sub-groups, each with its own short list of items (e.g. tools grouped by category) — is still a cards job at heart, just one level deeper: render each group as its own labeled cluster of small tiles, one tile per item, optionally with a logo (or a placeholder — see Design DNA) rather than a nested bullet list. Reach for this instead of "heading + paragraph with a bulleted sub-list" whenever the supporting content is itself enumerable.
- **Numbered rows / agenda / row-per-item** (`add_numbered_rows`, from `agenda.jpg`). One row per item — a two-digit brand numeral (or a design-system icon) at the left, the claim as a bold sub-heading beside it, the detail in `--content-static-secondary` under the claim — in **one column for 3–4 items, two columns for 5–8**, filling down the first column then the second. Three surfaces (`style`): **panel** (each row on a gray surface — the agenda), **ruled** (no surface; the list bracketed by a hairline above the first row and under every row — the reference contents page, with an optional right-aligned `meta` per row such as a page number or a duration), and **bare** (no surface, no rule — icon rows beside a quote or a paragraph in a split). A contents slide is ruled rows; a trust list is panels; four one-line claims next to a testimonial are bare. The tone split is what makes it skimmable — never produce that effect with inline bold inside a sentence. This is the densest honest way to fit 5–8 items that each need a sentence, and a distinct silhouette from a card grid, which is why an at-a-glance summary, an agenda and a trust/compliance list belong here rather than in a fourth row of cards. Past eight items, split the slide.
- **Pills.** The `badge`/pill atomic is the right element for tags, stage labels, status markers and the compact process strip described under Flow/graph — short standalone labels. It is not a container for content: a node that has to carry a caption, or an item that has to carry a paragraph, needs a real surface, not a widened pill.
- **Where the label goes.** Above the row when it labels a **comparison set** (the reader needs it before reading across); below when it is a **caption** describing what they just looked at. Pick one per slide and hold it.

### Quantitative & proof
- **Numbers / stats.** Title + a `.grid` row of 2–4 panels, each = a big **brand-purple** figure (Display scale) + bold caption + supporting paragraph. Built from `numbers`. **This is the one card-like recipe exempt from the icon/number-badge anchor rule** — the stat figure itself is the anchor. It is *not* exempt from the height rule, though: a stat card still sizes to its own content and centers in its row rather than stretching — see Design DNA "Surfaces." **A stat card's bottom padding is triple its side padding** (72px against 24px): the last line of text never sits on the card's floor, and the card's height includes that room rather than squeezing the text to make it (user correction 2026-09-21 — the earlier "deeper floor only when there is no description" rule was not enough). A stat card with no description line adds a little more still. Note: `numbers.css` paints `.numbers__value` in `--content-static-primary`; for the deck's brand-purple figure set the value colour to `--content-static-brand`. That override recurs — a candidate to graduate into a `numbers` brand-figure variant. **Four parts make a statistic**: the number, what it means, optionally progress against a target, and its **source** — a caption-scale line in `--content-static-tertiary`. Drop the source and the number stops being evidence; credit everything the same way (data sources, methodology notes, photo credits). **Round aggressively** — "$266K", not "$266,006.40"; precision belongs in an appendix. A **comparison figure** (last year's number, the market baseline) may sit beside the live one at the same type size in `--content-static-tertiary` — two tiers of metric, one size, no second accent colour; it is the stat-card form of the purple-vs-neutral series rule under Charts. Numbers can also be the *whole* slide: a hairline-ruled grid of figures with their captions, no panels and no chart furniture — a legitimate alternative to a row of stat cards once there are more than four. Two bare forms from the reference set, both on-token: the **rule stat** (`style="rule"`) — the Display figure with a solid 3px brand-purple vertical rule at its left, label in body type, no card — stacked one under another beside a paragraph (the split's `stats` panel, or `add_stats(style="rule")` as a ladder down the left half); it reads as editorial evidence rather than a KPI row, and is the right form when a slide has one paragraph and two numbers. And the **fact ladder** (`facts` panel kind) — six to twelve small supporting figures, each value bold in the heading face with its label inline after it ("11 238 athletes"), in one or two columns; for the numbers that are texture, not headlines, under a lead paragraph in a stack.
- **Charts.** Title + a `.grid` row of panels, each holding one simple chart (donut, bar pair, arrow/pentagon). **Purple = the primary/highlighted series; white/outline = the comparison series.** Caption paragraph under each. Compose chart shapes from token-colored elements. Four rules carry more than the chart type does: **headline the finding, not the data** (the title says what to conclude — "Rework costs two weeks per launch" — while the axis says what is plotted); **annotate the marks and kill the legend** (series labelled on the mark, value at the end of the bar, gridline values inside the plot, categories ordered by value — the only honest legend exceptions are repeating stacked bars and a 2–4 slice pie with its values inside the slices); **one accent series, everything else neutral** (the purple/outline rule above); and **put the value on the object** — if the reader has to look away from a mark to learn what it means, the chart failed.

  Chart forms built from rectangles, circles and text, and therefore on-token:
  - **Bar-in-track** — equal-length pale tracks, a filled brand-purple portion, the value in inverted text inside the fill. The best pattern for ranked percentages.
  - **Proportional colour blocks** — columns whose widths encode the split, label on top, the percentage large at the bottom.
  - **Stat bands** — full-content-width horizontal bands, figure left, caption right, one band per metric; one band may be an image instead (see Imagery).
  - **Drill-down bar** — a bar plus a second bar breaking out one of its segments, joined by dashed leaders, for "of that slice, most is X".
  - **Unit chart** — one square or icon per unit with a partial unit for the remainder; only under ~30 units.
  - **Proportional circles** with a right-aligned value ladder, instead of nested TAM/SAM/SOM boxes.
  - **Labelled scatter** — dots coloured by category, label beside each dot, for many items across two dimensions.

  Portability note: several of these (anything with curves or diagonal leaders) don't reduce to native Slides shapes — see `skills/smartcat-deck/SKILL.md`. Flag them rather than approximating them with the wrong shape.
- **Key metrics / OKR.** A row of "KR" cards (white number badge + text) above a "Steps established" sub-heading and a row of numbered step cards. Step number badges may use semantic green. Compose from panels + badges.
- **Highlights vs lowlights.** Two panels side by side, each = heading + a semantic icon badge top-right (green thumbs-up / red thumbs-down) + bulleted list. Compose from panels + semantic-colored atomic badges.
- **Testimonial.** A brand-gradient quote panel (large white quote) stacked over a gray attribution bar: circular photo + name/title on the left, customer logo on the right. Built from `testimonial`, reshaped to the stacked two-part card. Two different situations pull this into an **asymmetric split with a stats panel** instead of standing alone — both produce the same `add_split` shape, and both are still one idea per slide, not an exception carved out of that rule:
  - **The quote's own wording carries the number.** When the quote's wording carries distinct, quantifiable metrics that anchor its credibility (a number that *is* the value proposition — "increased productivity by 50%" — not just narrative color), pull those numbers out into their own stat card(s) beside the quote: quote text on one side (bold pull-quote styling + attribution, no gradient panel needed), 1–2 stat cards on the other, each repeating one number the quote already named.
  - **An aggregate proof slide and an unrelated customer quote proving the same claim.** A stats row drawn from several customers ("50% more productivity," "400% faster turnaround," "70% lower costs") and a quote from a different customer are one idea, not two, when they both support the same underlying claim — typically some form of "customers get the results they came to Smartcat for." The numbers are the quantitative half of that idea and the quote is the qualitative half; merging them is one idea per slide correctly applied, not a workaround around it. This is the general case behind decision-procedure step 1's merge rule below, not a special case of the bullet above: **the quote does not need to repeat the stats' own numbers, only support the same claim they do.**
  The split's `quote` panel is the **rule quote**: bold heading-face text with a solid brand-purple vertical rule at its left and the attribution in brand colour below — the reference form beside icon rows or stats (no gradient panel). Two quotes on one slide are a **staggered pair**: one top-aligned, the other `valign: "bottom"`, in a 6:6 split, so they read as two voices rather than one block.
  Keep the quote as plain, standalone prose (the original stacked-panel form above) when neither applies: a genuinely dedicated customer spotlight (a named account's own case study, given its own slide deliberately for visual weight), a quote that is pure narrative with no metric nearby to pair it with, or — the real reason to keep two slides — **a quote making a genuinely different point than the stats** (the stats prove cost savings, the quote is about ease of onboarding): that actually is two ideas, and merging it would blur the slide rather than sharpen it. "They're different content types" is not, on its own, that reason — a stats row and a quote are different *shapes* of evidence for one claim, which is exactly what an asymmetric split exists to hold.

### People & imagery
- **Roster** (`add_roster`, from the reference team and thought-leader slides). A grid of people — circle-crop portrait (a labelled dashed placeholder until the photo lands), name bold, role secondary — in three columns for 3–12 people, with the title in a left column and an optional aside under it. The portrait is the system's sanctioned circle-crop person photo; never a square headshot or a full-bleed portrait.
- **Image mosaic** (`add_image_mosaic`). Two to four reserved image slots composed as one object — same radius, same 8px gutter — in one of three layouts: **1+2** (one large square left, two smaller squares stacked right; an optional paragraph in the room that remains), **2×2** (four equal squares), **row** (two or three squares across). Because the promo shots are square, every cell is square. This is the slide for "here are the surfaces" — a product's four screens on one slide — and the third host for the promo set after the image-led split and the image divider.

### Comparison & relationships
- **Comparison v1 (side by side).** Two panels: neutral gray (dark text) vs **brand-purple gradient** (white text, Smartcat monogram top-right). The purple side reads as "ours"/preferred. Each = heading + bullets.
- **Comparison v2 (2×2 with flow).** Two stacked rows of the v1 pairing, with a downward chevron between the two brand panels to show progression.
- **Alignment matrix.** Two columns of numbered cards — **purple numbers = Smartcat (left), black numbers = client (right)** — with center labels between them and inward-pointing chevrons. "Smartcat" label top-left, client logo top-right.

### Time & structure
- **Timeline.** A horizontal center bar (alternating light/dark purple segments); milestones alternate above and below with dotted connectors; each = bold title + muted "(Day N)" + paragraph. Compose from token-colored bar + positioned text blocks. **For 4–6 plain milestones, a two-column ruled list (date │ event) usually beats the graphic** — it reads faster and it survives export to native Slides shapes. Keep the horizontal timeline for when the *shape* of the schedule is the point: overlaps, phases, or a long run of milestones.
- **Gantt.** A table with a Workstream/Description column and a numbered week or named month axis; process bars span columns — **solid purple = completed, light outline = future** — with a Completed/Future legend. Month variant adds dotted vertical gridlines.
- **Flow / graph.** When the flow IS the slide's whole content (e.g. "How it works" in N steps), build it as a row of **icon-bearing cards connected by dotted arrows** — every connector in this system is a **dotted line with an arrowhead**, never solid (user correction 2026-09-21; matches every reference diagram). Each node is a small card (icon + label, optional one-line caption), not a bare pill, and on its own slide the nodes floor at 40% of the room under the title, text top-aligned, so the chain earns the canvas. A chain is a legitimate slide by itself; it is **not** to be merged with the deck's feature cards — see "Stacked composition." Reserve the bare brand-purple pill node for a *secondary*, compact appearance: a short process strip stacked above a supporting detail-card row that carries the real content (see "Asymmetric & stacked layouts" above) — there, the pills read as a compact index into the cards below rather than needing to fill the slide on their own. Node boxes/circular nodes connected by dotted arrows, optionally grouped under "Timeline point" columns divided by baseline rules, remain the fuller treatment for a flow that genuinely branches or needs milestones grouped under headers.

### Tabular
- **Table.** Bold column headers, thin row dividers, muted values. Highlight the key column with a gray fill (e.g. the label column of a pricing table); add a bold summary/total row below when relevant. Compose from token-styled table markup. **Tabular figures** (`font-variant-numeric: tabular-nums`) in every column of numbers, here and in any stat ladder — proportional digits leave a column of figures ragged. One table worth naming as a role of its own: the **reader-routing matrix** — "who you are / what you care about / what you'll learn", one row per audience. It turns a generic document into several personalised ones for the cost of a single slide, and belongs near the front of a read deck.

### Closing
- **Back cover.** The deck's last slide and the cover's bookend: the **cover gradient** again, a short sign-off at Display scale top-left ("Thank you!"), the cover's own metadata line repeated bottom-left in pink-40, and the Smartcat wordmark bottom-right. It is the only slide besides the cover that carries the wordmark, and the only other place the cover gradient is allowed. Every deck ends with one; it follows the closing slide below rather than replacing it — that one makes the ask, this one signs off. Built with `add_back_cover`.
- **Closing / thank-you.** Title top-left ("Questions? Thank you."), on a brand-gradient, dark, or gray background — mirror the cover's treatment. Optional contact info or next-step lines, set as plain text with **real hyperlinks on the call-to-action phrases** — link colour plus underline, **not `.btn`**. The engine sets the deck's *theme* hyperlink colour to purple-50 `#8A59FC` (`--content-link-default` on dark), because PowerPoint and Google Slides paint links with the theme colour and ignore the run's own — the first build shipped Office-blue links for exactly that reason (user correction 2026-09-21) (see "No interactive UI" in Design DNA). The two standard links, set 2026-09-21: **"book a demo"** → `https://www.smartcat.com/book-a-demo/` and **"start a free trial"** → `https://smartcat.com/sign-up`; `add_closing` links either phrase automatically wherever a closing line contains it. Build from `heading`. An **author or contact chip** — circle-crop photo + name + role + an email address as plain text — is the sanctioned way to make a sent deck feel like a person wrote it. Still no button, and still only one accent job on the slide.

---

## D. The decision procedure — turning content into a slide

**Pass 0 — the deck before the slides.** Four checks, cheap now and expensive later:

- **Read or presented?** Decide before designing anything. A deck that gets *sent* — a business case, a proposal, a QBR left behind — carries real paragraphs, roughly 100–250 words a slide, with sources and footnotes. That is the density this catalog assumes and Smartcat's usual case. A deck *presented live* runs nearer 25 words a slide and lets the speaker carry the rest. Thin the copy for a presented deck, not the layout: the same recipes, fewer words per panel.
- **The headline test.** Write every slide's headline in order and read only those. They should make the argument on their own. If they don't, no layout will save the deck — fix the headlines before designing a single slide. It is also the fastest way to find the slide that isn't pulling its weight (step 1 below).
- **Does the deck breathe?** Never three dense slides in a row without a quiet one — a statement, a divider, a single stat. Together with the light/dark banding in section B, that rhythm is what makes a long deck scannable.
- **Skeleton before skin.** Block out each slide as bands and column spans first — what sits where, what is left empty — and only then apply tokens, colour and imagery. A layout that doesn't work in grey doesn't work.
- **The silhouette pass.** Once every slide has a headline and a shape (step 3 below), write the two side by side — `headline · silhouette · theme` — and read the silhouette column alone the way you read the headline column: no two adjacent slides the same, no shape on more than a third of the content slides, at least four shapes in a deck of eight or more, themes in bands of 2–4 with neither over 80%. Fix it on paper; `check_layout_variety` and `check_theme_banding` will only confirm what this pass already caught.
- **The product-shot pass.** If the deck is about one of the covered products, print the promo catalog first and mark which slides will carry which shot (Design DNA, "A deck about a covered product plans its slides around the shots that exist"). A shot with no slide is a slide you have not thought of yet.

Run these in order for each slide:

1. **Does this content deserve to be its own slide?** Two checks, in this order.
   **First, is the slide next to it making the same point?** Two adjacent slides arguing one thing are one slide. The clearest case is proof: a row of customer stats and a customer quote are both evidence for one claim — typically some form of "customers get the results they came to Smartcat for" — so they belong together, quote on one side and the figures backing the same claim on the other, as an asymmetric split (see "Testimonial" in section C for both ways this comes up). Merging them this way IS one idea per slide, not an exception to it: the numbers are the quantitative form of the idea, the quote is the qualitative form, and neither is a second, competing claim. Ask this before anything else, because merging two slides changes what each of them needs to contain. (The same logic runs the other way when one slide carries two genuinely different arguments — that is two slides.)

   Two objections come up here, and both mistake this merge for an exception to one-idea-per-slide rather than a normal case of it. **"They're different content types, each already sized to fill their own slide"** is not evidence they need separate slides — sizing to fill a slide on its own is just the single-role recipe's default, not a requirement; the split panel's own sizing rules (a stat figure steps to H1 below ~260px, a quote sets as bold pull-quote prose with no gradient panel — see Design DNA "Surfaces" and the Testimonial recipe) exist specifically so the merged form doesn't fall below the same legibility floor the standalone forms use. **"It blurs one-idea-per-slide pacing into two beats"** gets the rule backwards — the test was never "is this one visual element," it's "is this one claim." Four different numbers in a stats row are already several beats read as one idea; a quote saying the same thing in a customer's own words is the same move, not a second beat. The test to actually run is **do the stats and the quote support the same underlying claim?** If yes, merge — that is one idea per slide, correctly applied. If the quote is making a genuinely different point (the stats prove cost savings, the quote praises onboarding), keep them apart — that is two ideas, not one split across two content types. Only once they share a claim does overflow become the practical question: if the combined content doesn't fit the split's two panels at their documented floor sizes, split it — but that's a rare constraint, not the default reason to keep two slides.
   **Then, is what remains thin?** Check it isn't so thin it would leave most of the canvas empty — a bare title + one short sentence, a single stat with nothing else, a pill chain with no supporting row. If it is, fold it into an adjacent slide under one shared idea instead of giving it a slide of its own — see Design DNA, "A slide needs enough content to earn the canvas."
2. **What is this slide's *job*?** Map the intent to a role in section C:
   - Open / divide a section → cover / section divider
   - Land one idea → one big statement
   - Explain in prose → heading + paragraph (with a short supporting list, if needed) / N columns
   - Enumerate parallel items, however short → N cards / icon cards / grouped tiles / agenda
   - Quantify or prove → numbers / charts / key metrics / testimonial
   - Contrast options → comparison v1 or v2 / alignment matrix
   - Show sequence or plan → icon-card flow chain / timeline / Gantt / flow-graph
   - Present rows of data → table
   - Close → thank-you

   Whatever role you picked, also ask: **does this slide's content name a specific product surface** — a UI view, a conversation, a before/after, a dashboard? This is orthogonal to the role above, not a role of its own, and it applies to every one of them, including a single step inside a flow chain. If yes, reserve an image slot as part of building this slide, right now — not a review pass afterward, and not conditional on already having the screenshot (Design DNA, "A slide about a product surface reserves room for it"). A flow-chain step naming a real UI moment may be significant enough to earn its own slide (an `add_image_banner`) rather than staying folded into a node caption. `check_missing_image_slots` (in `deck_pptx.py`) is a mechanical backstop for this exact question — written for the case where the rule is known and simply never gets asked while planning — but it is a backstop, not a substitute for asking it here.
3. **Tailor the shape to the content's parts — list them first.** Before picking a builder, write down what this slide's content actually consists of, as parts: a claim, a paragraph, N parallel items (and how much copy each carries), a number or several, a quote, a sequence, a table, a product surface. **The shape is whatever holds *all* of those parts, not the builder that fits the first one.** A slide with a paragraph *and* a screenshot is an image-led split, not a paragraph with the picture dropped; three stats *and* a quote proving them is a stack; a five-step process *and* what each step delivers is a flow over cards. Then read the parts against the silhouettes:

   | The parts | Silhouette | Builder |
   |---|---|---|
   | One argument in prose, maybe a supporting fact | statement | `add_statement` |
   | One thesis sentence with 100–200 words of detail behind it | statement | `add_statement` (aside as paragraphs, `divider`) or `add_manifesto` |
   | A paragraph that introduces what follows | prose | `add_heading_paragraph` |
   | A paragraph plus two numbers that prove it | split | `add_split` text · `stats` with `style="rule"` |
   | Six to twelve small supporting figures | stack | `add_stack` text (lead) over `facts` |
   | A contents page or a list with page numbers / durations | rows | `add_numbered_rows(style="ruled")` with `meta` |
   | Short one-line claims beside a customer quote | split | `add_split` rows `style="bare"` · `quote` |
   | Two customer quotes | split | `add_split` quote · quote (`valign: "bottom"` on one) |
   | People — a team, speakers, authors | roster | `add_roster` |
   | Several product screens at once | mosaic | `add_image_mosaic` |
   | A chapter opener that deserves more than a title | divider | `add_section_divider` with `number` / `items` / `image_caption` |
   | 2–3 parallel items; or 4 with a line each | row | `add_cards` |
   | 4 items with real paragraphs; 5–6 items | grid | `add_cards(columns=2)` / 5–6 cards |
   | 3–8 short claims each with a line of detail (agenda, summary, trust list) | rows | `add_numbered_rows` |
   | A title, a paragraph and a product surface | split (image-led, title in the column) | `add_image_split` |
   | A product surface + cards or stats about it | split (image-led, full-width title) | `add_image_split` |
   | One headline element + a larger block of detail (narrative + card grid; a stat + a table; a quote + the stats it names) | split | `add_split` |
   | A headline element over the detail behind it (stats + quote bar; a chain + the screenshot of one step; rows + stats) — one idea, never a process plus a feature list | stack | `add_stack` |
   | 2–4 numbers with nothing else to say | row | `add_stats` (better: stack it over the quote that backs it) |
   | A sequence that is the whole point | flow | `add_flow_chain` — its own slide; the feature cards go on theirs |
   | Rows of data | table | `add_table` |
   | A 2:1-or-wider screenshot that is the whole argument | banner | `add_image_banner(aspect=…)` |

   Item count narrows the options; it never hands you a single answer. Respect the "whitespace is deliberate" invariant — if it's getting cramped, split into two slides. Respect the opposite too — "a slide needs enough content to earn the canvas" (step 1) and the fill floor (Design DNA) — if a shape is coming up as a strip in an empty canvas, the content wants a denser shape or a neighbour, not padding.

   **None of this makes the even card row or a plain paragraph a worse choice** — for content that's actually parallel or actually a single idea, they're the correct answer, not a fallback. The failure mode isn't using them; it's reaching for the same one on every slide. The silhouette pass in Pass 0 is where that gets caught, and `check_layout_variety` is the backstop for when it isn't.
4. **Where does this slide sit in the deck's rhythm?** Pick theme + background per section B — alternate light/dark in bands, reserve gradients for dividers and the open/close.
5. **Compose it** from tokens + atomic components (section A + the recipe). Reshape components as needed; keep every color/size/space/radius a token.
6. **Sanity-check against the DNA (A):** right title placement (cover = bottom-left, else top-left); period-style headline where it fits; semantic colors only for real positive/negative; brand gradient only on a hero moment; panels styled per light/dark; generous whitespace; **every card carries an icon or a numbered badge, except stat cards**; **every card's height matches its own content, with the row centered when it leaves extra vertical room** — never stretched or pinned-with-dead-space-below; **any missing image or logo asset is a labeled placeholder, not an omission**; **a flow that is the whole slide's content is icon-cards-with-arrows, not bare pills**; **for every gap between adjacent elements, check whether both sides have a surface — gutter (8px) if both do, slide padding (48px) if either is bare**; **no `.btn` anywhere — any call to action or link is plain/link-styled text.** Then the deck-wide ones: **no chrome — no page numbers, running heads, per-slide logos or rails**; **exactly one accent job, and no label above any title**; **every figure rounded and every data slide credited with its source**; **a reserved image slot wherever the slide names a product surface — and nowhere else — sized to the image it will hold, with the shot list reported on handoff**; **the content block fills at least half the space under the title, or the shape changes**; **this slide's silhouette differs from its neighbours', and its theme follows its band**; **no device used on this slide that isn't used elsewhere in the deck** — used once it is decoration, used throughout it is a system, and a recurring reshape graduates into a documented component variant (see the top of this doc).

When two roles both fit the content equally well, choose the one that needs the least visual machinery — a clean heading + paragraph beats a forced diagram. This is a tie-breaker between options that already fit, not a reason to skip step 3's check of whether an asymmetric or stacked shape fits *better* than the even default.

---

## F. Editorial patterns adopted from external references

On 2026-09-21 the user supplied 33 external reference slides (`C:\Users\isizo\Desktop\Deck refs` — Linear, Google Cloud, DEPT, INEOS, SPACE10, Research Ireland, It's Nice That, Further Forecast, several brand-guideline and portfolio decks) with two goals: stop generated decks looking repetitive, and make them read as enterprise-grade. Each slide was inspected and its devices sorted into *adopt* (does not contradict a system rule; folded into A–D above and built into the engine) or *reject* (contradicts a rule; listed so it is not re-proposed).

**Adopted — layout and grid.** A ~40/60 two-column editorial split with the thesis left and the detail right, separated by a hairline (statement + aside with `divider`); the headline-as-content manifesto with two narrow body columns beneath; a panel's own H3 sub-headline inside a split; the chapter-numeral divider with a ruled list of contents (numeral, title and list all top-anchored — the reference's lower-band title was rejected, see below); a full-height image panel on a divider (adopted *inside* the padding, see rejected); the 3-column people roster with a left title column; image mosaics of two to four equal-radius, equal-gutter cells; hairline-ruled contents rows with right-aligned page numbers; bare icon rows beside a rule quote; a staggered pair of quotes at different vertical anchors.

**Adopted — hierarchy and typography.** The five-level type stack (Display · H1 · lead H3 · body · caption) and the lead line as the entry point of every text-led slide; body copy at a 45–65 character measure in columns, never full width; the rule stat (Display figure beside a 3px brand vertical rule, no card) and the rule quote; the fact ladder of many small bold-value + label lines; the icon + label feature mini-grid under a product paragraph; muted secondary for all running copy with full primary reserved for headings, leads, values and names.

**Rejected, with reasons.** Full-bleed photography and half-bleed image dividers (every division stays inside the 48px frame — CLAUDE.md "Deck layout grid"); eyebrow labels above titles, including the purple sentence-case kind ("What is Linear?") and numbered trend pills ("Trend 01") — eyebrows are banned system-wide; running footers, vertical side rails, slide indices and progress dots — no chrome; an italic serif or monospace accent word — third family; marker-pen highlights on a phrase — banned inline emphasis; text set over photographs with colour fields behind it — type over an image goes on its own surface or the canvas splits; gradient blooms and glowing orbs as decoration — the blob ban; a decimal contents numbering ("1.0 About") — cosmetic, not a pattern; giant 200px+ chapter numerals — off the type scale (the numeral is Display, 64px); a divider's title and contents sitting in the lower band with the top of the slide empty — only the cover and back cover place content low, everything else anchors top-left (user correction 2026-09-21). Regular-weight large paragraphs at 24–32px were the one pattern the system genuinely cannot reproduce — there is no regular H3 token — so the lead level takes the heading face instead.

---

## E. Extending the brain

This doc is meant to grow as real example slides are provided. When given a new example (or a correction):

1. **Inspect it visually** (render to an image and look — don't infer from text).
2. **Extract, in this order:**
   - **Background & theme** — which of section B, light or dark.
   - **Layout skeleton** — title position, how the canvas is divided (columns/rows/panels), what's left empty.
   - **Atomic pieces used** — which atomic/existing components, and how they were *reshaped* (e.g. horizontalized card, mixed sub-components).
   - **Tokens** — the specific color/type/spacing/radius roles in play, especially any semantic or brand-gradient use.
   - **Anything new** — a pattern not already in section C.
3. **Fold it in:** refine an existing recipe in C, or add a new role; adjust A or B only if the example reveals a genuine invariant. Keep recipes short and composition-based. Log it below.

Do **not** move deck reasoning into `CLAUDE.md` — that file holds only the hard, binding rules; the how-to-think stays here.

### Changelog
- Initial version — derived from visual inspection of the "Smartcat deck template 2026 – All Designs" reference deck (106 slides): design DNA, background/theme axes, slide-role catalog, decision procedure.
- Test-driven refinements — built and rendered a sample composition slide ("What slow localization costs you"): named `--content-static-brand` as the stat-figure token; added the "recurring reshape → component variant" principle. Same test fixed a deck-grid bug: `.deck-slide` now zeroes column padding so grid content sits flush to the 48px slide padding (see `base/deck-layout.css`).
- Correctness fix — a token audit (ahead of a full generated deck) found the "brand + `data-theme=dark`" guidance from the prior refinement was wrong: `*-inverted` background tokens are self-contained pairs with `--content-static-inverted`, already correct in either ambient theme; combining with a theme flip breaks contrast. `base/deck-layout.css` and the background table above corrected — no code elsewhere had shipped this yet.
- Rule added — a full 10-slide test deck used real `.btn` components for the closing slide's CTAs. Corrected: decks are presented/viewed, not operated, so the button paradigm doesn't belong on a slide at all. Added "No interactive UI — ever" to Design DNA, updated the Closing recipe and the decision procedure's sanity-check to require plain/link-styled text instead of `.btn` for any call to action or link.
- Layout-variety correction (2026-09-18) — a generated deck was reviewed against `references/decks/` (the source reference images, not this doc's summary of them) and every content slide turned out to be either a single heading+paragraph or an even row of cards. Inspecting real examples — `exec-summary-3col.jpg`, `math-savings.jpg`, `compliance-risk-grid.jpg`, `pricing-table.jpg`, `stat-cards-quote.jpg`, `choice-cards.jpg` among them — showed real decks use those two shapes plenty, but also routinely reach for an **asymmetric split** (an uneven-ratio two-panel layout — a short narrative beside a wider card grid, one highlighted stat beside a table), a **stacked composition** (two different components in one slide, e.g. a process chain over a supporting card row, or a quote over its attribution bar), and **hierarchy within a repeated component** (one card in an otherwise-even row visually promoted as the featured option). Added a new "Asymmetric & stacked layouts" subsection to C, a hierarchy note to the N-cards recipe, a "simplest form" note to Flow/graph, and a new Design DNA bullet naming these as real options. Rewrote decision-procedure step 2 (D) to lay out per-situation options instead of one item-count → one-layout mapping.
  **Explicit correction, since the first pass at this got it backwards:** an even card row and a plain heading+paragraph are not problems to design around — they're the correct, most common answer whenever content is genuinely parallel or genuinely a single idea, and both recipes stay exactly as documented above. The actual defect was building slide after slide by reflex, on the nearest available shape, without asking each time whether *this* slide's content is asymmetric or layered enough to want something else. The fix is a deliberate per-slide choice among the options in C/D, not avoidance of cards or heading+paragraph — see step 2's explicit note.
- Component-fit correction (2026-09-18) — reviewing a generated deck against user feedback found a second, different failure mode from the layout-variety one above: several *individual components* didn't fit their own content. A one-sentence slide sat mostly empty; a flow chain rendered as bare pills with the bottom half of the slide blank; N-cards stretched panels to the full slide height even when the text inside was two lines, leaving a dead gray block below; several cards carried text only, with no icon or number to anchor them; a categorized tool list read as a plain nested bullet column instead of grouped tiles; and a 6-item enumerable list was rendered as a bullet column instead of cards. Added: a new Design DNA invariant that a slide must have enough content to earn the canvas (fold thin slides into a neighbor); a card-anchor rule (icon or numbered badge on every card, stat cards exempt) folded into "Surfaces"; a card auto-height + vertical-centering rule (size to content, center the row instead of stretching or pinning to top); a placeholder convention for missing image/logo assets (dashed panel + the `placeholder` icon + a caption naming the asset). Demoted "Bulleted list" from a standalone whole-slide role to a supporting element only — a genuinely enumerable, parallel list is now always a cards job (added a "Grouped tiles" recipe for categorized/nested cases). Rewrote the Flow/graph recipe so icon-bearing cards connected by arrows is the default when the flow is the slide's whole content; the bare pill chain is now documented only as the compact form paired with a supporting detail-card row underneath (unchanged from before). Added a new decision-procedure step 1 ("does this content deserve its own slide?") ahead of the existing steps, and expanded the sanity-check step with the new anchor/height/placeholder/flow rules.
- Quote-with-embedded-stats addition (2026-09-18) — a reference before/after pair (an independently regenerated version of a thin, prose-only testimonial slide) showed the quote's own two "50%" claims pulled out into separate stat cards beside the quote text, instead of standing alone as centered prose. The general rule behind that specific fix: a quote's wording carries a number worth its own card only when that number *is* the value proposition (a metric anchoring credibility), not whenever a quote happens to contain a digit — a quote that's narrative or qualitative stays plain prose, in the existing stacked-panel Testimonial form. Folded into the Testimonial recipe above as an asymmetric-split variant (quote text + 1–2 stat cards). Also corrected an inconsistency the same review surfaced: the stat-card anchor exemption added in the prior entry was being read as exempting stat cards from the height rule too — it doesn't; only the icon/number-badge requirement is waived, so `add_stats` now sizes and centers its row exactly like `add_cards` does.
- Cover recipe grounded in real references (2026-09-18) — `references/decks/cover/` was added with three real Smartcat cover slides. Pixel-sampling their backgrounds (identical across all three) found a specific vertical dark→purple→pink gradient, not the generic horizontal brand gradient the Cover recipe previously assumed; named it the "Cover gradient" in Design DNA → Color, cover-only. The same sampling caught that the existing brand-gradient bullet's pink hex (`#DA00FE`) doesn't exist in `tokens/globals.css` — corrected to the actual closest token, pink-70 (`#c326ed`). The three references also disagreed with the prior "headline bottom-left" placement rule (title actually sits in a lower-middle band, never flush at the bottom edge) and revealed the logo is never optional (always top-left; alone for a generic asset, paired with the client's logo for a named account) and that a cover's closing metadata takes one of three forms (top-right proposal info, a bottom two-column prepared-by/for block, or a simple footer line) or none at all, never more than one. Rewrote the Cover recipe and the Design DNA "Title placement" bullet accordingly. **Carried into `deck_pptx.py` the same day**: `add_cover()` now draws the cover gradient (raw multi-stop `<a:gradFill>`, since python-pptx's own gradient API only exposes 2 stops), the Smartcat wordmark via a new `draw_logo()`, an optional client logo or placeholder beside it, and all three metadata variants. The wordmark is embedded as a picture rather than reconstructed as native vector geometry like the icons — two of its letterforms need `fill-rule="evenodd"` to render their counters correctly, which OOXML custGeom has no per-path equivalent for, so a vector reconstruction risked silently filling the counters in solid. The real SVG was rendered once via an actual browser (`skills/smartcat-deck/assets/smartcat-logo-{white,black}.png`) instead.
- External-recommendation review (2026-09-20) — a 111-item list of general deck-design recommendations was reviewed against this system and filtered: the 57 that don't contradict an existing Smartcat rule were folded in above (many trimmed or reframed to land on tokens), the other 54 dropped. Kept and added: token-only leading and tracking and a hard two-family limit (Type); micro type as caption-scale tertiary in one fixed place; one accent job and one chromatic idea per slide; the headline accent word (matching the document-cover and social-canvas device); a scoped tonal ramp on the brand layer tokens; the emphasis ladder with two-tone title/paragraph; check-badge anchors, bento sizing and equal image crops (Surfaces); a new Imagery invariant (sanctioned visual types only, never stock, fix the ground not the type); an explicit **no chrome** invariant; the opening hook and at-a-glance summary roles; row-per-item, pill scope and label placement (Enumerated); the four parts of a statistic, aggressive rounding and a tertiary comparison figure (Numbers); headline-the-finding, kill-the-legend and seven on-token chart forms (Charts); tabular figures and the reader-routing matrix (Table); the ruled milestone list (Timeline); the contact chip (Closing); and a new **Pass 0** in section D — read vs presented, the headline test, deck breathing, skeleton before skin.
  **Trimmed on review the same week (2026-09-21):** three of the above were dropped again at the user's direction — the deliberate empty band, the four-sizes-per-slide ceiling with hand-set headline breaks, and the accent label above a title. The last one was a genuine error on the way in: a coloured sentence-case line above a heading is still an eyebrow, and eyebrows are banned system-wide regardless of casing or colour. Two reference slides carry one; they predate the ban and are not to be copied.
  **Rejected, with reasons, so they don't get re-proposed:** percentage-based margins and a separate tighter "chrome" margin (the 48px padding is measured from the real template, and Smartcat slides have no chrome at all); a 13:1 type ratio, 0.9–1.05 display leading, 1.4–1.55 body leading and 10–12px micro type (all off the token scale — display:caption tops out near 4.6:1 and every leading value is a token); a mixed-style headline with an italic serif or monospace chip, and letterspaced small caps (third/fourth families; small caps are a casing treatment the text-casing rule bans); large-and-light stat figures with superscript units (no light weight exists; figures are bold Display); cream grounds, per-chapter colour identity and colour-classified callout boxes (the palette is neutrals + one brand accent; multi-hue classification is a document-tier device); heavier-looking reversed type corrected with extra leading and a lighter weight (component CSS never changes per theme — only token values do); underlining a number (underline means link here), marker-pen highlights, outline/ghost type and bold lead-ins (inline emphasis, banned — use a real sub-heading element and tone instead); corner-anchored covers, an 8–15× title:metadata ratio and vertically-centred cover titles (the Cover recipe is already grounded in three real references); bottom-anchored statements and low-set body at ~45% (the title anchors top-left and the 80px heading→content gap is fixed); the half-and-half seam, bleeding an object off one edge, cropping type past the frame and diagonal splits (all break the 48px padding or the rectangular 12-column grid — diagonal *weighting* was kept, the geometry wasn't); a repeated identity mark on every slide, and the whole chrome-and-navigation section — running heads, corner kits, index tabs, edge spines, interactive PDF nav (absent from every reference, and interactive nav also breaks "No interactive UI"); "no bullet dots, ever" (the system's own rule is sharper where it matters: 3+ parallel peers are cards, and comparison/highlight panels legitimately use bullets); dividers carrying section numbers or mini-contents, and endings that ban "Thank you." (both contradict reference-grounded recipes and the house headline voice); hanging quote marks (a dropped pattern — see `.doc-pullquote`); book typography for forewords, outlined staggered persona cards, and a mandatory joke. Redundant rather than wrong, so also dropped: fixed-canvas design, symmetric margins, empty-state placeholders, and "a system is skeletons plus sanctioned variants" — all already rules here.
- Engine fixes found by rendering, not reasoning (2026-09-21) — a real 14-slide deck built from a smartcat.com product page was exported to PNG through PowerPoint itself (COM automation; the skill had assumed no local renderer existed on this machine) and every slide was looked at. Four defects that `check_overflow` cannot see, all now fixed in `deck_pptx.py`: **(1)** a slide title that wraps to two lines collided with the content below it, because every builder started content at a fixed `PAGE_PADDING + HEADING_GAP` measured from the title's *origin* — `_add_slide_title` now returns the content top, measured from the title's last line, and all seven builders take it; **(2)** `add_split`'s cards/stats panels re-centered themselves vertically while its text/quote/table panels anchored at the top, so a mixed split never lined up — panels now share one top edge, and the pair centers as a block only when both sides are measurable rows; **(3)** table value columns were right-aligned, which ragged-lefts any wrapped text cell — `pricing-table.jpg` left-aligns every column, so that is now the rule; **(4)** tables stretched to fill the slide height, ballooning five rows to ~100px each — rows now size to their own content and the table centers in the space below the title, the same rule card and stat rows already followed. Also widened the closing slide's gap between the Display title and its contact lines from 6pt to 40px. The lesson worth keeping: the programmatic checks passed clean on all four defects — only looking at the rendered slides caught them, so render before reporting whenever the environment can.
- Slide-by-slide review pass (2026-09-21) — the rendered 14-slide test deck was reviewed frame by frame and nine edits fed back into the rules and the engine. **Colour:** the cover's subtitle is purple-40 and its metadata line pink-40 (both already primitives — the cover's two small lines are tinted, not neutral); dark-mode panels are gray-80 `#1F1D2A`, not the lighter step they were; and `--content-static-brand` is now honoured as theme-aware in the engine (purple-50 on dark), since purple-70 on near-black was barely readable. **Type:** body copy sets at 1.15 leading and never at pptx's "single"; a card narrower than ~260px drops its heading to H4, and a stat figure on one drops to H1 rather than wrapping its last glyph out of the card. **Surfaces:** no drop shadows on anything, ever (PowerPoint was adding them by default — the flow arrows shipped with one); the icon anchor is a larger disc than the numbered badge and is white on any non-white card in either theme; both anchors take a bigger gap down to the heading; a stat card with no description line gets a deeper floor. **Composition:** a split's bare-to-paneled gap widens to 64px and the two panels center as one block (superseding the top-anchoring added the same week); and decision-procedure step 1 now asks first whether the neighbouring slide makes the same point — a stats row and the quote backing it are one slide, not two. **New role:** `add_back_cover`, the cover's bookend, which every deck now ends with.
- Reserved product-UI image slots (2026-09-21) — decks carried no imagery at all; a set of reference decks (`Deck refs/Image placement`) was reviewed for where product shots actually sit. Added a **reserved image slot**: a deliberate layout reservation for a screenshot inserted by hand later, kept strictly separate from the existing missing-asset placeholder (different purpose, different drawing — the slot is brand-purple and states its exact pixel size and ratio, because someone has to produce an image of that shape). Three shapes, all observed in the references: a side panel in a split (the default, 3:2), a full-width banner, and a card-row cell. Decks **embed** the real screenshot like documents do — never redraw product UI from atomics, which is the social tier's rule. Slots go only where a slide names a specific product surface, never on every slide. `image_slots(prs)` lists them for the handoff shot list, which is now a required step: a dashed box reaching a customer is worse than no image. Two caught during the build itself — a slot was stretching to its neighbour's height instead of holding its declared proportion, and `_aspect_label` was rounding a 3.95:1 banner to "16:9", both of which would have sent an exporter after the wrong crop.
- Correction: reserved slots are prospective, not conditional on having the asset (2026-09-21) — a session running the stale installed skill copy (predating image slots entirely) skipped adding any screenshots to a deck and volunteered its own reasoning: that placeholders are only for a "concrete shot you intend to drop in later," so a deck with no screenshots in hand should have no slots at all. That sentence is not, and was never, in this document — it was invented — but the "a deck with dashed boxes is worse than one with no images" phrasing above was loose enough to be read that way. Reworded both affected bullets: the obligation a reserved slot carries is **disclosure on handoff**, not avoidance. Lacking the real screenshot at build time is the normal, expected case — arguably the *only* case, since a deck is built before anyone goes and shoots product UI for it — not a reason to withhold the slot.
- Correction: the stats+quote merge needed its framing fixed, not just a rule (2026-09-21) — a session merged a deck's stats row and testimonial into one slide as this brain doc already instructed, but another session, building a similar deck, kept them as two and volunteered a plausible-sounding justification: different content types each already sized to fill a slide, and merging would blur one-idea-per-slide pacing into two beats. The first pass at fixing this framed the merge as an exception to one-idea-per-slide, which was itself corrected: the merge IS one idea per slide, correctly applied, not a carve-out from it — the one idea is a single claim ("customers get the results they came to Smartcat for"), with the stats as its quantitative form and the quote as its qualitative form. Restructured the Testimonial recipe (section C) to name two distinct situations that produce the same merged split (the quote's own wording carries the number, vs. an aggregate stats row and an unrelated quote proving the same claim), added the real test for keeping two slides (a quote making a genuinely different point than the stats — that actually is two ideas), and rewrote decision-procedure step 1 so the test for merging is always "do they share a claim," not content type or beat-counting.
- Ruled lists are bracketed, roomy and icon-led (2026-09-22) — a divider's chapter list rendered as four tight lines with rules only between them, which read as underlined sentences. Now a rule sits above the first row and under every row, rows are 56px, and each may carry a muted 20px icon; `add_section_divider`'s `items` take `{"label", "icon"}` as well as plain strings, and `add_numbered_rows(style="ruled")` was bracketed the same way.
- Title, paragraph and screenshot is a horizontal split (2026-09-22) — a rendered slide put a full-width title and a line of copy over a slot filling the rest of the canvas, which came out as a wide flat box no square screenshot could fill. The user's fix, now the default: the title joins the copy in the left column and the shot runs the full slide height on the right. `add_image_split` gained `title_column` (auto-on for a bare text panel), and `add_image_banner` delegates to it unless given a genuinely wide `aspect`, so the flat-banner-with-a-paragraph slide can no longer be built by accident.
- Cards beside a screenshot stack vertically (2026-09-22) — a rendered slide showed two cards side by side next to a full-height image slot, each a tall narrow column with two lines of text and a floor of empty gray. The user's fix, now the rule and the engine's default for 2–3 cards whose block is far taller than they need: stack them one above the other as full-width cards. Clarified the relationship to the earlier horizontal-card correction — stacked when it fits, horizontal only when the stack would overflow.
- Two corrections from the first deck built with the new patterns (2026-09-21) — a divider carried its title and contents list at the bottom of the slide with the top empty (the reference lower-band form): rejected; dividers now stack numeral, title and list from the top-left like every non-cover slide. And the closing slide's hyperlinks rendered Office-blue: the theme's hyperlink colour is now set to purple-50 on every new deck, since Office ignores the run colour on linked text.
- External references, second set — 33 slides studied for layout, hierarchy and type (2026-09-21) — see the new section F for the full adopt/reject list. Built into the engine: `add_manifesto`, `add_roster`, `add_image_mosaic`; `add_section_divider` gained `number`, `items` and `image_caption`; `add_statement` takes a list of aside paragraphs, a `ratio` and a `divider`; `add_split` takes `divider`; every split/stack panel may carry a `heading`; the text panel gained `lead`, paragraph lists, `features` and `valign: "bottom"`; the quote panel became the rule quote; `stats` gained `style="rule"`; `rows` gained `style="ruled"|"bare"` and per-item `meta`; a new `facts` ladder panel. Two silhouettes added (`mosaic`, `roster`). Folded into Design DNA: the five type levels with the lead line, and the reading-measure rule for body copy.
- Review of the rebuilt deck — seven corrections from the user (2026-09-21, same day as the pass below) — the user went through the rendered `content-translator-coworker-v2` slide by slide. **(1)** Text ran out of the bottom of the three stacked cards beside a screenshot: the cards were squeezed to share the image's height. Rule added — text never leaves its box; the engine now reshapes a card set that would not fit instead of squeezing it, and `check_text_fit` (in `run_all_checks`) estimates every text box against its room. **(2)** For that case the card should be horizontal, icon left and text right: added the `horizontal` card layout, the default for any single column of cards. **(3)** Bullets continuing a paragraph were set in white while the paragraph was gray: bullets take the paragraph's colour; the general dark-mode rule (headings and key captions white, all running text secondary) is now written down. **(4)** Flow connectors become dotted, as in every reference diagram. **(5)** A process chain and the feature cards had been merged into one stack: two ideas, two slides — the stack recipe now says so and names the one case where cards may sit under a chain. **(6)** Stat cards take triple bottom padding, included in the card's measured height. **(7)** The closing slide's "book a demo" and "start a free trial" become real hyperlinks to the two standard URLs, applied automatically by `add_closing`.
- Layout variety, third pass — this time with numbers and builders (2026-09-21) — the user reviewed a 15-slide generated product deck and found it still repetitive and predictable, with the promo screenshots in `images/promo-ui-mockups/` barely used (two empty slots, zero fills, while four finished shots of exactly that product sat in the folder). The contact sheet showed the actual defect, which the two earlier variety passes had described but not fixed: **eleven light content slides, each a title over one squat strip of equal boxes floating mid-canvas** — cards ~120px tall at caption size in ~520px of room. Three root causes, three kinds of fix. **(1) Every builder produced the same silhouette**, and the shapes the references use most were not buildable at all — so `deck_pptx.py` gained `add_statement` (statement-body.jpg), `add_numbered_rows` (agenda.jpg), `add_stack` (stat-cards-quote.jpg, workflow-steps.jpg — panel kinds now include `quote-bar`, `rows`, `flow`), card **grids** (`add_cards` up to 6, `columns`, a `featured` card; 2×2 inside a split too), and `add_image_split`. **(2) "Size to content" had no floor**, so cards shrank to strips: a `MIN_CONTENT_FILL` of half the room under the title now applies to every content block, card copy sets at body size on any card 360px or wider, and image slots grow to match their neighbour. **(3) Nothing asked the question at build time** — so, the same lesson as the image-slot entry above: every builder stamps a *silhouette*, and `check_layout_variety` (no adjacent repeats, no shape over a third, at least four shapes), `check_theme_banding` (no theme over 80% of content slides) and `check_canvas_fill` (nothing under 40%) run in `run_all_checks` on every build. The skill's own "light sandwich" rule turned out to be a cause of the flatness (content light, only furniture dark) and contradicted both this doc and the references, which are majority dark on content slides; it is replaced by banding. **On the images:** all 17 promo shots are 1080×1080 squares, which no existing slot shape suited — a 3:2 side panel or a banner covers half the slot. `add_image_split` sizes the slot to the image (a full-height square, copy beside it, vertically centred), `fill_matched_slots` does the whole lookup in one call with the filename-tag matching from CLAUDE.md, `list_promo_images` prints the catalog at plan time, and the report names unused product shots and poor fits. Rebuilt the same deck with the same copy as `output/decks/content-translator-coworker-v2.pptx` — 6 distinct silhouettes, 5 dark / 4 light content slides, 3 of 4 product shots placed — for a like-for-like comparison with the original contact sheet. Rules folded in above: Design DNA "Silhouette variety is a rule with numbers" and "A content block earns the canvas or changes shape", the reworked slot shapes and the product-shot planning bullet, the theme paragraph in B, new/expanded recipes in C (statement, numbered rows, stack, image-led split, card grids), Pass 0's silhouette and product-shot passes, and step 3 rewritten as "list the content's parts, then pick the shape that holds all of them."
- Correction: a known rule that never got checked needed a mechanical backstop, not more wording (2026-09-21) — a deck built with this skill shipped with zero reserved image slots despite a flow-chain step captioned "Side by side, per language" sitting right next to a slide about reviewing content. Asked why, the session correctly recited the rule ("a screenshot slot belongs wherever a specific product surface is named, and not having the screenshot yet is never a reason to skip it") — it had simply never run the check while planning, because nothing in the actual planning steps asked the question; it lived only as a Design DNA invariant and one clause inside a 15-item sanity-check sentence at the very end of the decision procedure. Wording fixes don't close a process gap, so three changes: the question is now asked explicitly inside decision-procedure step 2 ("what is this slide's job") and SKILL.md's Step 2 planning arc, at the point the slide list is built, not after; and `deck_pptx.py` gained `check_missing_image_slots(prs)`, a mechanical scan for screenshot-inviting language ("side by side," "before and after," "in context," "the dashboard," and others) run on every build alongside `check_overflow`, required in SKILL.md Step 5. Reproduced the exact reported miss as a test (a flow-chain node captioned "Side by side, per language") and confirmed the new check flags it. One gotcha the new function's own docstring calls out: it must run on the in-memory `prs` from the same build, before saving — the slot-tracking attribute it checks against is a runtime attribute, never written into the saved `.pptx`, so running it against a reloaded file would flag every slide with matching language whether or not it originally had a slot.
